[metadata]
name = bmms-changelet
version = 0.1.0
author = Your Name
description = ChangeSet schema + validator + converter
license = MIT

[options]
packages = find:
package_dir =
    = src
python_requires = >=3.9

[options.packages.find]
where = src
